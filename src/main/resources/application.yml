spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wischgym?useSSL=false&serverTimezone=UTC
    username: root
    password: 621458
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
